# 1.0.1

- App fullscreen when opened first time
- Delete __pycache__ from commit
- Update main_window (remove multi video joiner)
- Change `clipbot` to `generate voice over`
- Add new ui tab: `audio_video_combiner`
- Add new worker: `combine_audio_video`

# 1.0.2
Update:
- Add new workers: `workers/assign_caption.py` and `workers/generate_caption.py`
- Create folder `stt-python-wrapper`
main function is to wrap fast-whisper function into exe, so main *.exe can run it by subprocess. All of created functions still in development.
- Remove `faster-whisper` in requirements.txt and move it into stt-python-wrapper\
